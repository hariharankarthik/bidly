Optional auction sounds (add MP3 files here — the app will no-op if missing):

- bid.mp3 — played when your bid succeeds
- timer-warning.mp3 — at 10s and 5s on the countdown (host + guests)
- gavel.mp3 — when the host hammers a sold lot
- sold.mp3 — reserved for future use (e.g. alternate sold sting)

See lib/sounds.ts for paths wired in the UI.
